doctype html
html
    head
        title Error
        link(rel='stylesheet' href='css/style.css')
    body
        .container
            h1 Error
            h2 #{message}
            if error.status
                h3 Status: #{error.status}
            if error.stack
                pre #{error.stack}